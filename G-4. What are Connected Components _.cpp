#include <iostream>
using namespace std;

/* there can be multiple components in a same graph
   which are not connected.
   
   to traverse on every components we make visited 
   array
*/


int main() 
{
	int visited[n+1]={0};
	
	/* initialy all nodes will be 0.
	we will make nodes 1 , as we visit 
	nodes
	*/
	
	//we will traverse for every unvisited nodes
	
	return 0;
}
